THAWING MEMORY
BUILD JOURNAL

This journal records the actual development process for the Critical Reflection. Add one entry after each meaningful work session or implementation step. Record problems and changes honestly rather than rewriting the process as a smooth sequence.

Entry template

Date:
Planned task:
What was completed:
Choices made and why:
What failed, changed, or surprised me:
Evidence, such as commit, screenshot, prompt output, or bug:
Next small step:


ENTRY 001

Date: 31 August 2026
Planned task: Define the prototype scope before visual design or implementation.
What was completed: A lean prototype PRD and an interaction and prompt specification were created. The assessed milestone was reduced to one complete Kitchen memory loop.
Choices made and why: The user memory comes before AI output. The model produces only a provisional reflection, limitation note, and follow-up question. Keep, edit, and reject choices keep the user active, while the original model response remains visible for accountability.
What failed, changed, or surprised me: The original proposal planned three functional domains. The narrower milestone better matches the assessment requirement for one reliable core interaction.
Evidence, such as commit, screenshot, prompt output, or bug: docs/prototype-prd.txt and docs/interaction-prompt-spec.txt. Commit to be added.
Next small step: Review and commit the planning documents, then remove the Vite demonstration content and create the unstyled landing-page shell.


ENTRY 002

Date: 31 August 2026
Planned task: Review the complete static interaction from the landing page to a saved Living Archive entry.
What was completed: The landing, Kitchen form, empty validation, static AI response, keep/edit/reject controls, local saving, Archive rendering, and persistence after refresh were tested as one continuous browser flow.
Choices made and why: The test used fictional memory text. The original AI response remained visible while user-authored revisions and corrections were displayed first in the Archive.
What failed, changed, or surprised me: An edited trace could initially be saved without changing the AI text because the edit field was prefilled. This could incorrectly label unchanged AI writing as the user's version. Save validation was changed so an edited trace must differ from the original AI reflection.
Evidence, such as commit, screenshot, prompt output, or bug: Browser review completed with no console warnings or errors. The validation change is in src/pages/Kitchen/KitchenPage.jsx. Commit to be added.
Next small step: Define and apply the prototype visual system to the complete static loop.


ENTRY 003

Date: 1 September 2026
Planned task: Establish and verify the local reflection endpoint before connecting Gemini.
What was completed: A Node HTTP server now accepts POST requests at /api/reflect, validates the known card ID and memory, enforces request and memory limits, and returns a clearly temporary three-field response.
Choices made and why: Node's built-in HTTP and fetch-compatible environment were selected to avoid adding a server framework for one prototype endpoint. The React client remains disconnected until the server contract and Gemini response are independently reliable.
What failed, changed, or surprised me: A previous test server continued holding port 3001 after its terminal session ended. Windows blocked detailed process inspection without elevated access, so the updated endpoint was safely verified on port 3002 instead of expanding permissions.
Evidence, such as commit, screenshot, prompt output, or bug: Valid request returned 200. Empty memory, unknown card, overlong memory, invalid JSON, oversized payload, and wrong HTTP method returned the intended structured errors. The server log did not contain submitted memory text. Commit to be added.
Next small step: Create a Gemini API key locally and replace the temporary server response with validated live model output.


ENTRY 004

Date: 1 September 2026
Planned task: Connect the static Kitchen loop to a live free generative model and verify the assessed core interaction.
What was completed: Gemini 3 Flash Preview now returns a validated reflection, limitation, and sensory question. The React interface calls the local server, preserves memory through failures, retries temporary provider errors once, and saves completed traces to the Living Archive.
Choices made and why: The API key remains in the local server environment. Gemini 3 Flash Preview was selected because it worked through the free AI Studio project. A second provider was deferred because a reliable primary loop, clear error handling, and source separation were more important than adding another integration.
What failed, changed, or surprised me: Gemini 2.5 Flash-Lite appeared in the model list but generation returned provider errors for the project. Gemini 3 Flash Preview initially returned temporary 503 errors, so one controlled retry was added. A general recipe question also showed that the model could infer user intention from very little evidence.
Evidence, such as commit, screenshot, prompt output, or bug: Successful live outputs included a yogurt chicken-curry memory and a Chettinad recipe request. Multiple traces persisted in the Living Archive. Lint and production build passed.
Next small step: Decide whether the AI follow-up question should become a real conversational turn or remain a one-shot prompt.


ENTRY 005

Date: 1 September 2026
Planned task: Resolve the conceptual gap between the AI's follow-up question and the user's inability to answer it.
What was completed: A bounded conversational reconstruction was specified. The user may answer or skip up to three AI questions, then ask the system to organise only their fragments into a Working Family Recipe with visible unknowns.
Choices made and why: The conversation is bounded rather than unlimited. Previous AI text is never treated as recipe evidence. The model acts as interviewer and organiser, while the user remains the source and final authority.
What failed, changed, or surprised me: A conventional regional recipe assistant initially seemed like a useful extension, but it would shift the model into the role of cultural authority and conflict with the research argument. The revised design supports recipe formulation without allowing the model to invent missing cultural knowledge.
Evidence, such as commit, screenshot, prompt output, or bug: docs/conversational-reconstruction-spec.txt and decision-log entry 006.
Next small step: Implement the conversation data model and pure helper functions without changing the visible interface.


ENTRY 006

Date: 2 September 2026
Planned task: Add the live server path for a bounded continued reflection.
What was completed: The reflection endpoint now distinguishes the original one-turn request from the new conversation request. A valid continue action sends labelled user fragments, AI question context, and skipped questions to Gemini and expects a validated reflection response with an explicit type. The original one-turn path remains available.
Choices made and why: Only user-authored fragment text is presented as memory evidence. Questions previously written by the AI are labelled as context only so the model can avoid repetition without converting its own wording into family knowledge. Working recipe generation returns a clear not-yet-implemented response until its separate prompt and schema are built.
What failed, changed, or surprised me: The updated local endpoint passed validation and routing checks, but two live Gemini attempts timed out and returned the safe model-unavailable response. The failed call did not log the API key or memory text and did not require changing the continuation contract.
Evidence, such as commit, screenshot, prompt output, or bug: Fifteen automated tests passed, including new prompt tests for source separation and question repetition. Lint and the production build passed. The health route returned successfully on the temporary test server.
Next small step: Connect the Kitchen interface to the continued reflection request and add answer, skip, and turn-limit states.


ENTRY 007

Date: 2 September 2026
Planned task: Connect the Kitchen interface to the server-side continued reflection path.
What was completed: The user can now answer or skip the latest AI question, submit up to three follow-up memory fragments, retry a failed turn without duplicating it, and review earlier user fragments and provisional AI responses. The Open memory button now carries the same gold call-to-action pulse as the landing-page entry button.
Choices made and why: User fragments are stored before the request begins so a provider failure cannot erase them. Earlier AI turns are available for review but remain visually and structurally separate from user-authored evidence. The question prompt was narrowed toward operational recipe details after the original sensory-first wording produced a question that did not materially advance reconstruction.
What failed, changed, or surprised me: The technically successful one-turn interaction still felt conceptually inactive because paraphrasing and broad sensory prompting did not help formulate a method. Adding another model turn only becomes meaningful when its question targets a practical gap.
Evidence, such as commit, screenshot, prompt output, or bug: Fifteen automated tests, lint, and the production build passed. A live browser test still requires restarting the server process on port 3001 so it loads the new continuation route.
Next small step: Restart both local processes and test one complete two-turn reconstruction with fictional text, then implement the Working Family Recipe server response.


ENTRY 008

Date: 2 September 2026
Planned task: Reduce visual overload after a continued reflection and clarify the user's next action.
What was completed: On wider screens, the memory card now sits beside the accumulated user reconstruction, while the current AI response sits beside the continuation controls. The controls explicitly ask the user to choose one next step: answer, skip, or finish and evaluate. Evaluation remains hidden until requested.
Choices made and why: Pairing source material with its accumulation and model output with its controls makes the relationships easier to scan. Hiding evaluation prevents two different decision stages from competing at once. The layout returns to a single column below 55rem.
What failed, changed, or surprised me: Adding continuation functionality increased conceptual value but also made the original vertical page difficult to parse. The interface needed progressive disclosure, not simply more controls beneath the existing ones.
Evidence, such as commit, screenshot, prompt output, or bug: Fifteen automated tests, lint, and the production build passed.
Next small step: Test the paired layout with a successful two-turn live response, then implement Working Family Recipe generation.


ENTRY 009

Date: 2 September 2026
Planned task: Teach the conversational model through archive examples and make continued-request progress unmistakable.
What was completed: The Living Archive now includes separate demonstration conversations for kept, edited, and rejected outcomes. Each example exposes its user fragments, the practical AI question answered by each fragment, and expandable provisional AI turns. During a continued request, the existing AI response softens and pulses, its sections display staggered thinking dots, and the supplied loading flower travels from the continuation controls toward the response.
Choices made and why: Demonstrations are static project examples rather than records inserted into local storage, preventing fictional data from being presented as the visitor's archive. Loading motion is tied to the real request state and supplemented by a screen-reader status message. Reduced-motion preferences disable all guiding animation.
What failed, changed, or surprised me: The interface needed to demonstrate not only outcomes but also what a useful answer to an AI question looks like. Showing the questions beside the user fragments makes the intended reconstruction behaviour more legible than three isolated final cards.
Evidence, such as commit, screenshot, prompt output, or bug: The archive structure was reviewed in a browser. Fifteen automated tests, lint, and the production build passed.
Next small step: Live-test the full follow-up transition after restarting the server, then implement Working Family Recipe generation.


ENTRY 010

Date: 2 September 2026
Planned task: Make the visual handoff between a submitted follow-up and the next AI response complete and directional.
What was completed: The loading flower now travels once over 5.5 seconds from the continuation controls into the AI response and remains pulsing at its destination if the request takes longer. A minimum transition duration prevents fast provider errors from removing the guide halfway through. The reconstruction card slides from behind the memory card, and the first AI response slides down from beneath the memory area.
Choices made and why: The flower path and the request result are coordinated so motion communicates a complete transfer rather than functioning as an unrelated looping decoration. The response reveal runs only when the response area first appears, while later requests use pulse and loading feedback without repeatedly sliding the whole card.
What failed, changed, or surprised me: A rate-limit response could arrive before the original looping animation reached its destination, making the guide appear broken. Waiting for one bounded visual handoff resolves that mismatch while keeping the user's fragment preserved.
Evidence, such as commit, screenshot, prompt output, or bug: Fifteen automated tests, lint, and the production build passed.
Next small step: Retry a live continuation after the free Gemini limit resets, then implement Working Family Recipe generation.


ENTRY 011

Date: 2 September 2026
Planned task: Turn the corner flowers into a continuous guide between interaction stages.
What was completed: One persistent focus guide now measures the active card and animates both flower corners between the memory card, AI response, continuation controls, and evaluation. After a successful response, the guide rests on the AI response for four seconds before moving to the next user action.
Choices made and why: A single measured guide creates spatial continuity that separate decorations could not provide. The active target remains determined by interaction state, and the flowers remain decorative, non-interactive, and excluded from the accessibility tree.
What failed, changed, or surprised me: Recreating flowers on each card made the destination visible but did not communicate movement through the process. Preserving one guide across state changes makes the transition itself legible.
Evidence, such as commit, screenshot, prompt output, or bug: Fifteen automated tests, lint, and the production build passed.
Next small step: Inspect the moving guide during a successful live conversation after Gemini quota becomes available.


ENTRY 012

Date: 2 September 2026
Planned task: Complete the conversational payoff and make live generation reliable enough to demonstrate.
What was completed: Working Family Recipe generation now organises only user-authored fragments into remembered ingredients, method, sensory cues, family notes, and visible unknowns. Groq now provides automatic fallback after temporary Gemini failures. The travelling loading flower and its artificial delay were removed after the interface became visually overloaded.
Choices made and why: A recipe is an organisation of remembered evidence, not a model-completed recipe. Provider fallback is invisible to the interaction but disclosed in the privacy copy. Malformed output is not hidden by fallback.
What failed, changed, or surprised me: Gemini quota failures blocked repeated live tests. A live model also misread ammiikkal as something added to chammanthi, demonstrating that fluent follow-up questions can still misunderstand family terminology.
Evidence, such as commit, screenshot, prompt output, or bug: The chammanthi conversation is preserved as a demonstration archive entry. Automated tests cover provider fallback and user-only recipe evidence.
Next small step: Save complete version 2 conversations, clarify the three-stage hierarchy, and prepare the repository and reflection for submission.


ENTRY 013

Date: 2 September 2026
Planned task: Complete archive persistence and submission documentation.
What was completed: New traces preserve all fragments, AI turns, skipped questions, the working recipe, and the user's decision. Version 1 entries remain readable. The Kitchen shows a three-stage progress model. The default Vite README was replaced with project, setup, privacy, architecture, testing, and limitation documentation. A critical reflection draft was created from recorded build evidence.
Choices made and why: Existing user data is read rather than silently migrated or deleted. The README distinguishes demonstrated behaviour from future scope, and the reflection explicitly states that no participant testing occurred.
What failed, changed, or surprised me: The previous archive technically saved a result but omitted the conversation that now carries the project's argument. Persistence had to change when the conceptual core changed.
Evidence, such as commit, screenshot, prompt output, or bug: Version compatibility and complete trace construction are covered by automated tests. Lint and production build results are recorded in the final verification run.
Next small step: Conduct the final end-to-end verification and address any defects found.
